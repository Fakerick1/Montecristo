# Setup configuration variables
onServer <<- 0
#Rshiny
VERSION <- "1" # Current Version to display
BUILD <- "BUILDNUMBER" # Current build number, will be replaced by Jenkins so do not change the text "BUIDLNUMBER"

print("AppCode - configuration.R")
